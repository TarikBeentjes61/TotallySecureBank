import { fileURLToPath, URL } from 'node:url';
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

// https://vitejs.dev/config/
export default defineConfig({
  base: '/TotallySecureBank/', // Ensure this is the correct base URL for your deployment
  plugins: [
    vue(),
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  build: {
    outDir: 'dist', // Ensure this is the correct output directory
    sourcemap: true // Enable sourcemaps for easier debugging of build issues
  }
});